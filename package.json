{
  "name": "crystalcore-os-api-tiers",
  "version": "0.1.0",
  "private": true,
  "description": "Scaffold: API tiers, entitlements, usage ledger, and Stripe settlement stubs for CrystalCore.OS",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test tests/entitlements.test.ts tests/usageLedger.test.ts tests/stripeWebhook.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
